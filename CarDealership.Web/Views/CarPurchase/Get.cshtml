@model CarDealership.Domain.ReadModels.CarPurchase

@{
    ViewData["Title"] = "Car Purchase";
}

@await Html.PartialAsync("_CarPartial", new CarPartialViewModel
{
    Car = Model.Car,
    SalesPerson = Model.SalesPerson,
    Customer = Model.Customer,
    OrderDate = Model.OrderDate,
    SoldFor = Model.PricePaid
})